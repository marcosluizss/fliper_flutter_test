name: fliper_flutter_test
description: Projeto desafio para a Fliper

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter    
    
  equatable: ^2.0.0
  dartz: ^0.10.0-nullsafety.1  
  connectivity_plus: ^1.0.1  
  http: ^0.13.3
  bloc: ^7.0.0

  cupertino_icons: ^1.0.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  mockito: ^5.0.7

flutter:

  uses-material-design: true

